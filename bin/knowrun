#!/bin/bash
# insta485run

# Stop on errors
# See https://vaneyckt.io/posts/safer_bash_scripts_with_set_euxo_pipefail/
set -Eeuo pipefail

# else, run
flask --app KNOW --debug run --host 0.0.0.0 --port 8000